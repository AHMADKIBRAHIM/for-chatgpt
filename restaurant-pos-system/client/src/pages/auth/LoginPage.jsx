import { useDispatch } from "react-redux"; import { useNavigate } from "react-router-dom"; import LoginForm from "../../components/auth/LoginForm"; import { loginSuccess } from "../../features/auth/authSlice"; export default function LoginPage(){const d=useDispatch(); const n=useNavigate(); return <LoginForm onSubmit={({email})=>{d(loginSuccess({token:"demo-token",user:{id:"1",name:"Admin",role:"admin",email}})); n("/pos");}}/>;}
