import { useState } from "react"; import Input from "../common/Input"; import Button from "../common/Button"; export default function LoginForm({onSubmit}){const[email,setEmail]=useState("admin@pos.local"); const[password,setPassword]=useState("password123"); return <form className="card" style={{maxWidth:420,margin:"2rem auto",display:"grid",gap:12}} onSubmit={(e)=>{e.preventDefault();onSubmit?.({email,password});}}><h2>Login</h2><Input value={email} onChange={e=>setEmail(e.target.value)}/><Input type="password" value={password} onChange={e=>setPassword(e.target.value)}/><Button type="submit">Sign in</Button></form>;}
