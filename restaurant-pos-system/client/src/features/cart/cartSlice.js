import { createSlice } from "@reduxjs/toolkit"; const slice=createSlice({name:"cart",initialState:{items:[]},reducers:{addItem:(s,a)=>{const i=s.items.find(x=>x.id===a.payload.id);if(i)i.qty+=1;else s.items.push({...a.payload,qty:1});},removeItem:(s,a)=>{s.items=s.items.filter(i=>i.id!==a.payload);},clearCart:(s)=>{s.items=[];}}}); export const {addItem,removeItem,clearCart}=slice.actions; export default slice.reducer;
