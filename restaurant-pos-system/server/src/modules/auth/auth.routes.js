import { Router } from "express"; import { asyncHandler } from "../../shared/utils/asyncHandler.js"; import validate from "../../middleware/validate.middleware.js"; import auth from "../../middleware/auth.middleware.js"; import { loginSchema, registerSchema } from "./auth.validation.js"; import { loginController, meController, registerController } from "./auth.controller.js"; const r=Router(); r.post("/login",validate(loginSchema),asyncHandler(loginController)); r.post("/register",validate(registerSchema),asyncHandler(registerController)); r.get("/me",auth,asyncHandler(meController)); export default r;
